{
  "name": "sample-proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8787",
    "start": "wrangler dev --port 8787"
  },
  "devDependencies": {
    "itty-router": "^3.0.12",
    "wrangler": "^3.0.0"
  }
}
